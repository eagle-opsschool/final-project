{
  "ID": "{{ ansible_hostname }}-mediawiki-{{ mediawiki_port.stdout }}",
  "Name": "mediawiki",
  "Address": "{{ outside_addr.stdout }}",
  "Port": {{ mediawiki_port.stdout }},
  "tags": ["mediawiki"]
}
