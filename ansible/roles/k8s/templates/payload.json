{
  "ID": "{{ ansible_hostname }}-wikimedia-{{ wikimedia_port.output }}",
  "Name": "wikimedia",
  "Address": "{{ outside_ip.output }}",
  "Port": "{{ wikimedia_port.output }}",
  "tags": ["wikimedia"]
}
